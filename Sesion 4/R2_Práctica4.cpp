#include <iostream>
using namespace std;

int main (){
	
	// a)
	// La condición del if nunca se cumple puesto que tendrías que introducir la letra F tanto mayúscula como minúscula, se tendrían que cumplir las dos.
	// Se arreglaría cambiando el && (y) por un || (o) y así se entraría en el if tanto si es mayúscula como si es minúscula, se tendría que cumplir cualquiera de las dos.
	
	// b)
	// La velocidad no puede ser mayor que 100 y a la vez ser mayor que 70, ya que 70 es un número menor que 100, por tanto nunca va a entrar en ese if, no tiene sentido.
	
	// c)
	// Si la velocidad es mayor que 100, también es mayor que 110, por lo tanto ese "velocidad > 110" no tiene sentido.
	
}
